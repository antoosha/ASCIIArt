package ui.worker

class ConsoleWorker {

  // 1. get line from console
  // 2. parse arguments of this line
  // 3. work with arguments (load image -> convert image -> filter image -> export image)
  // 4. present info about success
}
